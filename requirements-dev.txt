# Dépendances de développement / tests.
# Installation : pip install -r requirements-dev.txt
-r requirements.txt

# Versions épinglées (== aux versions installées/testées) — même raison que
# requirements.txt (build reproductible, audit 2026-07-23).
pytest==9.1.1
# Mesure de couverture (constat superviseur 2026-07-23 : aucun projet de la
# flotte ne mesurait ce que ses tests couvrent). Voir conventions.md pour la
# commande. Pas de seuil imposé : on mesure d'abord.
pytest-cov==7.1.0
# Client HTTP utilisé par starlette.testclient (TestClient). Starlette >=1.3
# le préfère à httpx et émet sinon un DeprecationWarning.
httpx2==2.5.0
# Extraction de texte depuis les PDF générés (test_interview_pdf_export.py)
# pour vérifier le contenu réel rendu, pas seulement l'absence d'exception.
pymupdf==1.28.0
