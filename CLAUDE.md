# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

Interview-to-Deck: an internal tool for consultants running qualitative interviews during audits/studies. Pipeline: capture interviews — either structured against a trame (Mission → Trame → Theme → Question) or, since incr.9, freeform with AI-structured turns of speech and no trame — → cross-interview AI synthesis → PowerPoint restitution deck generated from a corporate template. Codebase and docs are French; domain vocabulary (`trame`, `synthèse`, `verbatim`, `recommandation`, `axe`) is established product terminology — keep it as-is, don't translate/anglicize.

Progress tracking lives in `.roadmap/roadmap.json` (rendered via the `roadmap-keeper` skill). Deeper onboarding docs are in `docs/wiki/` (architecture, conventions, stack), also viewable as a standalone rendered page at `docs/wiki.html` — both can lag behind the actual code/git history — verify against `git log`/`git status` rather than trusting either as current truth.

## Commands

```bash
# Setup
python -m venv .venv
.venv\Scripts\activate                 # Windows; .venv/bin/activate on POSIX
pip install -r requirements.txt -r requirements-dev.txt

# Run the dev server (auto-reload), http://127.0.0.1:8000/missions
uvicorn app.main:app --reload

# Tests (FastAPI TestClient + pytest, disposable SQLite DB — never touches data/app.db)
pytest -q
pytest tests/test_mission_trame_flow.py::test_export_pptx_produces_valid_file  # single test
pytest -k "pptx or export"             # subset by keyword
```

There is no linter/formatter configured (no ruff/black/eslint) and no pre-commit hooks — match the surrounding file's existing style.

## Architecture

**Request flow**: FastAPI route (`app/routers/*.py`) → service function (`app/services/*.py`) → Jinja2 template (`app/templates/`), server-rendered with HTMX 2.0 for partial updates (autosave fields, dynamic panels). No SPA/build step on the frontend.

**Data model** (`app/models.py`, SQLAlchemy 2.0 / SQLite): `Mission` owns at most one `Trame` (→ `Theme` → `Question`, optional since incr.9 — see below) and many `Interview` (→ `Answer`/`Verbatim` for a `mode="parametre"` interview that follows the trame). A mission also has one `GlobalSynthesis` (5 fixed categories: contexte, culture_adn, forces_succes, points_amelioration, aspirations) and several `RecommendationAxis` (→ `Recommendation`, scored 1-5 on valeur/complexité). The PPT export and the web "aperçu" editor are two different renderings of the same `GlobalSynthesis`/`RecommendationAxis`/`Recommendation` data — there is no separate "slide" entity.

**Unified entry point + libre interviews** (incr.9, `app/routers/entretiens.py`): `/` (not `/missions`) is now the landing page — 3 choices (entretien libre, entretien structuré, nouvelle mission). The first two create a `Mission.is_draft=True` row immediately (an `Interview`/`Trame` always needs a `mission_id`) with a placeholder name; its real identity is completed afterward via `/missions/{id}/finaliser` (`missions.py`) — either naming it, or reattaching its interviews/trame to an existing non-draft mission (only offered when that mission has no trame yet, since `Mission`→`Trame` stays 1:1; reattachment reparents via a raw `sqlalchemy.update()` + `db.expire_all()` before deleting the now-childless draft, to sidestep the ORM's `delete-orphan` cascade on a re-parented one-to-one). `Interview.mode` (`parametre`/`libre`, set at creation, never exposed on any update route — the "lock" is structural, not a runtime check) distinguishes the two: `libre` has no `Trame` at all, instead `InterviewTurn` rows (interlocuteur/question/remarque, plus an optional `section_title` marking the turn that opens a new topic — inherited by following turns, see `interview_export.py::group_turns_into_sections()`, shared between the Analyse screen and the per-interview Markdown export below) produced by `interview_libre_extract_ai.extract_libre_from_text()` — one AI call, on the raw transcript before human review, that structures the turns into sections, drafts a répartition into `GlobalSynthesis`'s 5 categories (stored as `Interview.repartition` JSON, same shape as `synthese_ai.GLOBAL_SCHEMA`), a 1-3 sentence `Interview.resume`, and detected identity — all reviewed together on one screen before saving. One read-only screen renders this as a document rather than a raw form (styled like the pre-existing `interviews/preview.html`, `.preview-doc` CSS): `GET /interviews/{id}/analyse` — turns grouped by section, then résumé + the 5 categories in prose, all on one page (built 2026-07-15 against two real example documents as a quality bar; merged from two separate screens into one on 2026-07-17 — see "Convergence libre/trame" below). `synthese_ai.generate_global_synthesis()`/`_build_global_prompt()` take an additional `material_libre` parameter (list of `(Interview, repartition)`, gathered by `synthese.py::_libre_material()`) injected as its own prompt section alongside `material_by_theme` — so a mission's global synthesis blends both interview types. Every template/route that assumed `mission.trame` always exists now guards for `None` (see "Mission sans trame" below for the 2026-07-17 fix that made this actually work end-to-end, not just avoid crashing). The saved audio backup (`Interview.audio_backup_path`, `data/recordings/`) is now actually served — `GET /missions/{id}/interviews/record/backup/{filename}` (path-traversal-guarded) — and linked from both record screens (appears once the backup upload completes) and `libre_detail.html`; before this it was written to disk but never exposed anywhere, a real gap a user caught in testing, not a regression in this repo's git history despite how it was reported. `audio_transcribe.py`'s Whisper defaults were raised `small`→`medium` + `beam_size` 1→2 (`WHISPER_BEAM_SIZE`, new) after a real-audio comparison (`tests/exemple/`, a ~114min recording with a human-edited reference transcript) showed `small`/greedy badly mangling proper nouns ("Michel Nakache"→"Michel Lacache", "Aurore"→"l'horror") — `medium`/beam=2 got them right; timing was inconclusive in the sandboxed dev environment, worth re-checking live-segment speed on real hardware. `ai_common.py` gained an `ollama` provider (plain `urllib` HTTP to a local server, no new dependency, no API key — `is_configured()`/`call_ai_json()` special-case it to skip the key check every other provider requires) so `interview_libre_extract_ai.py`'s analysis can run fully local, pairing with the already-local Whisper transcription; requires Ollama installed + a model pulled on the machine (not something this codebase can provision) — installed and tested end-to-end on the dev machine since 2026-07-17 with real audio (`llama3.1:8b`, `qwen2.5:3b-instruct`, `qwen2.5:1.5b-instruct` pulled). CPU-only inference is slow and is the confirmed bottleneck, not Whisper (`llama3.1:8b` ~55-70s for even a short paragraph; `qwen2.5:1.5b-instruct` ~4x faster, extraction quality trade-off unconfirmed) — `OLLAMA_TIMEOUT` and `OLLAMA_CHUNK_MAX_WORDS` (map-reduce chunk size, `interview_libre_extract_ai.py`) are both env-var-overridable (`.env.example`) for tuning per machine; a real timeout on a ~37min real interview was observed and is the main open perf question (see `docs/wiki.html`'s TODO table). Per-interview Markdown export (US9.7, `app/services/interview_export.py::build_interview_markdown()`, 2026-07-16): a "Télécharger (.md)" button on `libre_analyse.html` and `preview.html` downloads `GET /interviews/{id}/export/markdown` — one interview's own content (libre: résumé + structured transcript by section + the 5-category répartition; parametre: theme/question/answer/verbatim, like `preview.html`). Distinct from the pre-existing mission-wide `export.py::export_interviews`/`mission_export.build_export_markdown` (all interviews of a mission, theme-grouped, feeds the external-analysis-and-reimport circuit via `analyse_import.py`) — this one has no analysis-request template footer and isn't reimportable, it's a plain read/archive export. The independent UX/UI pass (US9.12, 4 parallel design-review agents, 2026-07-16) fed a round of concrete fixes through 2026-07-17 (wizard, autosave feedback, Q&A redesign, interview/mission synthesis decoupling, screen convergence below) — see `.roadmap/roadmap.json` increment 9 for the full list.

**Mission sans trame — synthèse/recommandations/export PPT (fix 2026-07-17)**: a mission with only libre interviews (`mission.trame is None`) used to 500 on `GET/POST /missions/{id}/recommandations` (`synthese.py`'s `recommendations_view`/`generate_recommendations_view` did `mission.trame.themes` unguarded) and, even once that's fixed, the 4 wizard screens (`synthese/recommandations.html`, `globale.html`, `apercu.html`, `export_import.html`) all gated their *entire* content — including the AI generation panels — behind `{% if not themes %}`, so a libre-only mission saw "la trame est vide" instead of its real synthesis even though `_all_theme_material`/`_libre_material` already fed libre material into it correctly. Both fixed: the guard is now `{% if not mission.interviews %}` (material presence, not trame presence). The legacy per-theme synthesis screen (`synthese_view`, `synthese/theme.html`/`_panel.html`, superseded since incr.9 by the Analyse→Synthèse globale→Recommandations→Export PPT wizard) was confirmed orphaned and removed rather than fixed.

**Convergence libre/trame — écran "aperçu" (2026-07-17)**: a trame interview has exactly 2 screens (edit `capture.html`, read-only `preview.html`); a libre interview had the same conceptual content spread over 3 (edit `libre_detail.html`, read-only turns `libre_analyse.html`, read-only résumé/répartition `libre_synthese.html`). Merged the two read-only screens into one (`libre_analyse.html` now renders turns-by-section then résumé/répartition, matching `preview.html`'s single-page shape) — `GET /interviews/{id}/analyse/synthese` is now a 308 redirect to `/analyse` for link compatibility, `libre_synthese.html` is gone. `libre_detail.html`'s button relabeled "👁️ Aperçu" to match `preview.html`'s convention.

**Régénération d'analyse + map-reduce synthèse globale + polish wizard (2026-07-18, TODO wiki items 1-3)**: `POST /interviews/{id}/analyse/regenerer` (`interviews.py`) relance `generate_repartition_from_turns()` sur les tours de parole *actuels* de l'entretien (pas la transcription brute — permet de bénéficier d'une correction de tour sans tout ressaisir), affiche le résultat sur un écran de revue dédié (`libre_regen_review.html`, ancienne valeur repliable en regard) et n'écrase `Interview.resume`/`repartition` qu'après confirmation explicite (`.../regenerer/confirm`) — les tours et l'identité ne bougent jamais sur ce chemin. `synthese_ai.generate_global_synthesis()` applique désormais le même map-reduce que `interview_libre_extract_ai.py` (découpe la matière aux frontières de thème/entretien via `ollama_chunk_max_words()`, synthétise tronçon par tronçon, fusionne par un appel de réduction dédié `GLOBAL_REDUCE_SYSTEM`) — corrige la cause structurelle d'un timeout réel observé le 2026-07-17 sur un entretien de ~37min (un seul prompt géant dépassait à la fois `OLLAMA_TIMEOUT` et la fenêtre de contexte du modèle) ; une mission courte ne fait toujours qu'un seul appel. `ai_common._call_ollama()` distingue maintenant un vrai timeout (`URLError(reason=TimeoutError)`, message actionnable citant `OLLAMA_TIMEOUT`/`OLLAMA_CHUNK_MAX_WORDS`/`SYNTHESE_MODEL`) d'un serveur injoignable (message générique précédent) — avant ce correctif les deux cas produisaient le même message trompeur "vérifiez qu'Ollama tourne" alors qu'il tournait, juste trop lentement. Polish wizard entretien libre : `POST /missions/{id}/interviews/record-libre/retour` et `.../retour-tours` repréremplissent l'écran précédent (transcription, ou tours de parole) depuis les champs cachés du formulaire courant au lieu d'un formulaire vierge — le bouton "Annuler" devient "← Revenir à …", aucun appel IA sur ce chemin ; et `POST /missions/brouillons/nettoyer` supprime en un clic les missions brouillon sans entretien ni trame remplie (accumulées car chaque entrée "entretien libre/structuré" en crée une), affiché sur `/missions` seulement si `nb_brouillons_vides > 0`, à côté d'un lien "Reprendre" vers `/finaliser` sur chaque brouillon restant.

**Revue UX + export PDF durci (2026-07-19)** : 7 correctifs remontés par une revue de structure du site. Le plus notable : la régénération IA de la synthèse globale/recommandations (`synthese.py::generate_global`/`generate_recommendations_view`, qui écrase `GlobalSynthesis`/tous les `RecommendationAxis` existants) n'avait aucun garde-fou contre un clic accidentel — corrigée par `hx-confirm`/`onsubmit confirm()` plutôt qu'un écran de revue dédié façon régénération d'entretien libre (disproportionné ici, le pattern confirm() existe déjà pour la suppression de mission). Autres correctifs : l'écran lecture seule d'un entretien libre (`libre_analyse.html`, route toujours `/interviews/{id}/analyse`) affiche désormais "Aperçu" partout (titre/breadcrumb/h1) pour matcher `preview.html` côté structuré — seule l'URL ne change pas, pour ne pas perturber la redirection 308 historique depuis `/analyse/synthese` ; les 3 écrans du wizard libre (`record_libre.html`, `libre_turns_review.html`, `libre_review.html`) ont un breadcrumb `Missions / {mission.name} / …` au lieu de `Nouvel entretien / …` (la mission brouillon existe déjà en base à ce stade) ; "Ouvrir la synthèse" sur le hub mission (`missions/detail.html`) route vers la bonne étape du wizard (recommandations si des axes existent, sinon synthèse globale si elle a du contenu, sinon export-import) au lieu de toujours repartir de l'étape 1 ; le libellé "Nouvel entretien" — partagé entre la nav globale, l'écran de choix `/` (dont un des 3 choix est "Nouvelle mission", pas un entretien) et l'écran de création d'entretien dans une mission — est désambiguïsé : nav + écran `/` renommés "Démarrer", `interviews/new.html` (création réelle d'un entretien) garde "Nouvel entretien". Indépendamment : `interview_pdf_export.py` traitait les retours à la ligne d'un texte utilisateur (réponse, note libre, remarque) comme un espace simple (reportlab `Paragraph` interprète le texte comme du mini-HTML) — une réponse multiligne s'affichait comme un bloc continu ; fix via un helper `_text()` (`escape()` + `.replace("\n", "<br/>")`) qui remplace tous les appels `escape()` du fichier. `tests/test_interview_pdf_export.py` (nouveau, `pymupdf` ajouté à `requirements-dev.txt` pour extraire le texte réel d'un PDF généré) couvre cet export pour la première fois, modes parametre et libre, cas dégradés compris.

**Timeout Ollama en usage réel — cause : démarrage à froid, pas la taille du prompt (2026-07-19)** : un utilisateur a bien reçu "Ollama n'a pas répondu à temps" sur l'étape répartition de l'entretien libre juste après un démarrage de serveur. Diagnostic en direct (`ollama ps`/`ollama list`, puis un appel Ollama chronométré à la main) : une fois le modèle chaud, un appel réaliste prend ~20s, très loin des 300s de `OLLAMA_TIMEOUT` — la génération n'est pas le goulot. Le vrai coût est le *chargement* du modèle (6 Go) en mémoire, payé au premier appel puis à chaque fois qu'Ollama l'a déchargé faute d'activité (5 min par défaut côté serveur Ollama — plus court qu'une pause de lecture entre deux écrans du wizard). Deux correctifs dans `ai_common.py`, tous deux couverts par `test_ai_common.py` et vérifiés en conditions réelles (redémarrage serveur + reproduction du flux : 34s, succès) : `warm_up_ollama()` précharge le modèle au démarrage du serveur (même principe que `audio_transcribe.warm_up()` pour Whisper, appelée dans `main.py::lifespan` avec le même garde-fou try/except — ne bloque jamais le démarrage si Ollama n'est pas encore lancé) ; chaque appel envoie désormais `keep_alive` (30 min par défaut, `OLLAMA_KEEP_ALIVE`, nouveau dans `.env.example`) pour qu'Ollama garde le modèle chargé entre deux clics de l'utilisateur plutôt que de le décharger après 5 minutes. Le message d'erreur de timeout cite maintenant aussi ce levier.

**Map-reduce sur l'extraction structurée + conservation de la transcription brute (2026-07-19)** : `interview_extract_ai.extract_answers_from_text()` — le chemin qui extrait les réponses depuis une transcription en **mode structuré** (`record_interview()` et l'import `.docx`, tous deux via `import_interview_confirm()`) — était le seul appel IA du projet sans aucun découpage, alors que le code lui-même note qu'« un entretien peut durer 1h-1h30 » : même risque de troncature/timeout déjà corrigé ailleurs (extraction libre, synthèse globale), resté intact ici. Corrigé par le même découpage par paragraphes que l'extraction libre — `_chunk_text_by_paragraph` déplacée de `interview_libre_extract_ai.py` vers `ai_common.chunk_text_by_paragraph()` (partagée, plus de duplication) — avec une fusion **sans appel IA supplémentaire** : la première réponse non vide trouvée pour chaque question l'emporte entre tronçons (une question posée une fois n'a pas besoin d'être recomposée, contrairement à un résumé qui couvre tout l'entretien) ; le comportement intra-tronçon (dernière occurrence gagne si l'IA répond deux fois à la même question dans un seul appel) est inchangé. Un seul tronçon = un seul appel, chemin court inchangé. Nouveau champ `Interview.raw_transcript` (migration additive dans `db.py`) conserve désormais la transcription telle qu'enregistrée — avant, elle ne survivait que le temps du formulaire (perdue après extraction, aucun moyen de la consulter/exporter après coup) ; rempli uniquement par le flux d'enregistrement audio (`identity["transcript"]`, jamais présent pour l'import `.docx`, où l'utilisateur garde déjà son fichier source). `interview_pdf_export.py` ajoute une section « Transcription brute » en fin de document quand le champ est renseigné — même route d'export existante (`GET /interviews/{id}/export/pdf`, déjà liée depuis `preview.html`), aucun nouveau bouton/écran nécessaire.

**Vérification réelle + export PDF de secours sur échec d'extraction (2026-07-19)** : sur demande explicite, le pipeline map-reduce ci-dessus a été rejoué pour de vrai sur le serveur avec Ollama réel (pas mocké) — transcript de 2113 mots (2 tronçons réels, ~300s) pour `extract_turns_from_text()`, 45 tours (2 tronçons + 1 fusion réels, ~329s) pour `generate_repartition_from_turns()` — les deux ont abouti avec du contenu cohérent, confirmant que le découpage tient « quel que soit la taille » au-delà de ce que les tests unitaires (mockés) prouvent isolément. Point important découvert au passage : ni le bouton « ■ Arrêter » ni le widget « 🧪 Test (temporaire) : Transcrire ce fichier » (`record_libre.html` uniquement, absent de `record.html`) ne déclenchent l'extraction IA — ils ne font qu'assembler le texte transcrit (Whisper) dans le textarea ; l'extraction se déclenche sur les boutons de soumission séparés (« Extraire questions/réponses », puis « Continuer vers la synthèse »). Nouveau garde-fou, sur les 3 écrans où une extraction IA peut échouer (`record.html`, `record_libre.html`, `libre_turns_review.html`) : un bouton « ⬇️ Télécharger la transcription (PDF) » apparaît à côté du message d'erreur, POST vers `/interviews/transcript/export-pdf` (nouvelle route, stateless — aucun `Interview` en base requis) → `interview_pdf_export.build_transcript_only_pdf()`. Avant ce correctif, un texte transcrit (parfois 1h+ d'entretien) resté bloqué par un échec IA n'avait aucune issue que la ressaisie. Vérifié en conditions réelles : échec Ollama réellement provoqué (`OLLAMA_HOST` invalide), écran d'erreur capturé, PDF téléchargé et son contenu confirmé.

**Vraie cause du timeout Ollama : la taille de tronçon, pas le démarrage à froid (2026-07-19, suite)** : le correctif warm-up/keep_alive ci-dessus n'a pas suffi — l'utilisateur a rapporté le même timeout quelques heures plus tard sur `tests/exemple/split_03.weba` (37min réelles, confirmé via `av.open().duration`). Re-mesure en réel, cette fois à la taille **réellement configurée** (`OLLAMA_CHUNK_MAX_WORDS`, 1800 mots par défaut), modèle **chaud** : ~570s, quasiment le double d'`OLLAMA_TIMEOUT` (300s) — la génération elle-même, pas le chargement, est le coût dominant sur ce matériel CPU. Mon premier diagnostic avait mesuré un appel chaud sur un petit prompt (21s) et conclu à tort que la chaleur du modèle suffisait ; ce test n'était pas représentatif de la taille réelle d'un tronçon de production (constat capitalisé côté superviseur, voir plus bas). Nouveau défaut `OLLAMA_CHUNK_MAX_WORDS=400` (mesuré ~150s à chaud, marge confortable) ; `_call_ollama()` relance désormais une fois un timeout isolé avant d'abandonner (pic de charge ponctuel, pas un troncon structurellement trop gros — dans ce cas les deux tentatives échouent pareil, le message oriente alors vers `OLLAMA_CHUNK_MAX_WORDS`). Vérifié en réel à la nouvelle taille par défaut : 149.5s, conforme à la prédiction.

**Limite structurelle à 3h — analyse technique (2026-07-19)** : extrapolation à partir des mesures réelles ci-dessus (`temps ≈ 32.7 + 0.291 × mots`) — un entretien de 3h (~24 300 mots) prendrait **~152min rien que pour l'étape transcript→tours**, en une seule requête HTTP synchrone bloquée. Le tronçonnage élimine l'*échec* mais pas le *temps total*, qui dépend du volume à traiter, pas de la taille des tronçons. Conclusion : l'architecture synchrone actuelle ne passe pas à l'échelle pour 3h, quel que soit le réglage — il faut un traitement asynchrone en arrière-plan (soumission immédiate + écran de statut qui interroge la progression + reprise sur échec au niveau du tronçon) pour un vrai support 3h. Analyse complète, options écartées et recommandation détaillée : `docs/reflexions/extraction-longue-duree.md`. Mitigation immédiate sans code : découper un entretien de 3h en plusieurs entretiens libres successifs (la synthèse globale de mission agrège de toute façon tous les entretiens).

**Diagnostic superviseur (étage 2, 2026-07-19)** : constat écrit sur la vérification incomplète du premier correctif ci-dessus (mesurée sur un prompt non représentatif) — proposition retenue et appliquée : `revue-increment/SKILL.md` §2 impose désormais de mesurer un correctif de timeout/perf IA à la taille maximale réellement configurée, pas un échantillon arbitraire (mémoire `feedback-ai-timeout-fix-verify-at-configured-scale`).

**Étude — enregistrement auto-segmenté en tranches de 30min (2026-07-19)** : suite de l'analyse 3h ci-dessus, sur demande explicite. Concrétise l'option « traitement asynchrone » en s'appuyant sur un mécanisme déjà existant et éprouvé (`record_libre.html`/`record.html` font déjà tourner un `MediaRecorder` de transcription toutes les 60s sans jamais couper le micro, `startSegment()`/`rotationTimer`) — proposition : appliquer le même pattern de rotation au *second* `MediaRecorder` (la sauvegarde audio complète, aujourd'hui uploadée une seule fois à l'arrêt) à un intervalle de 30min, sans aucune interruption perçue par l'utilisateur. La tranche de 30min devient l'unité naturelle d'un futur job de traitement en arrière-plan (assez longue pour amortir les allers-retours réseau, assez courte pour que son traitement — ~27min mesuré — ne prenne jamais de retard sur l'enregistrement de la tranche suivante). Étude complète (schéma JS, modèle de données `audio_segments`/`InterviewSegmentJob`, téléchargement par tranche, phasage en 2 paliers indépendants) : `docs/reflexions/enregistrement-segmente-30min.md`. Pas encore implémenté — étude livrée, décision de lancer le palier 1 (segmentation + téléchargement, risque faible) et/ou le palier 2 (traitement asynchrone, chantier substantiel) à trancher par l'utilisateur.

**AI provider abstraction** (`app/services/ai_common.py`): all AI calls (theme/global synthesis, recommendations, trame/interview extraction) go through one `call_ai_json(system, prompt, schema, json_hint, error_cls=...)` function. The active provider is chosen by `AI_PROVIDER` (**ollama**/openai/mistral, default ollama since 2026-07-17 — Anthropic support was removed entirely, no SDK dependency, no code path, at the user's request after hitting a billing/credit error) with no automatic fallback between providers — a missing key raises a clear error naming the exact env var expected (`api_key_env_name()`). `synthese_ai.py`, `trame_extract_ai.py`, `interview_extract_ai.py` each define their own system prompt/schema/error class but never touch an SDK directly. `SYNTHESE_DEMO=1` enables a rule-based offline fallback (no AI, no cost) used when no key is configured. `ollama` (incr.9, no new dependency — plain `urllib` HTTP calls to a local Ollama server, `OLLAMA_HOST` optional, defaults to `http://localhost:11434`) runs `interview_libre_extract_ai.py`'s analysis (turns/répartition/identity) without sending interview content to an external AI — pairs with the already-local Whisper transcription for a fully offline "entretien libre" pipeline. Requires Ollama installed and a model pulled on the machine; now installed on the main dev machine (`qwen2.5:1.5b-instruct`, `qwen2.5:3b-instruct`, `llama3.1:latest` pulled, 2026-07-17) — `SYNTHESE_MODEL` defaults to `llama3.1` for this provider, quality on 7-8B local models vs. hosted-frontier models for this kind of structured JSON extraction is still an open question worth watching in practice.

**PPT export** (`app/services/pptx_export.py` + `app/services/pptx_deck.py`): `build_presentation()` either starts from a client-uploaded `.pptx` template (inherits theme/masters/logo — `_clear_slides()` must drop each removed slide's relationship via `prs.part.drop_rel()`, not just clear `sldIdLst`, or PowerPoint silently refuses to open the file even though python-pptx parses it fine) or builds a blank 16:9 deck (in which case the native title placeholder, inherited from python-pptx's 4:3 default, is repositioned explicitly per-slide in `_new_slide()` rather than by mutating layout/master placeholders, which is unreliable). Every slide-building function pulls sizing from `pptx_deck.D.TYPE` (the one type scale) and adapts font size to content length via `D.ajuster_police`/`D.estimer_lignes`/`D.tronquer_a_lignes` rather than assuming fixed line counts. `build_presentation()` always ends with `D.verifier_geometrie()`, raising if any shape ends up off-slide. A `.pptx` that parses in python-pptx and passes this check can still fail to open in real PowerPoint or look wrong visually — always verify a real export with the `pptx-verify` skill (render + eyeball) before considering a layout change done. See the `pptx-deck` and `restitution-deck-design` skills for the design system this generator follows.

**Web editor / PPT preview parity**: `app/templates/synthese/apercu.html` is a tabbed editor (one tab per slide) whose form fields reuse the same autosave endpoints as the rest of the app (`/syntheses/globale/{mission_id}/field`, `/recommandations/{id}/field`, `/recommandations/axes/{id}/field`). The "shape-aware" hint shown under each field comes from `pptx_export.field_fit_hint()`, which reuses the exact geometry constants and font-fitting functions the real PPT generator uses — treat that coupling as intentional; if you change a slide's layout constants in `pptx_export.py`, update `FIELD_SHAPE` too or the hint will drift from reality.

**New (2026-07-15) — `pptx-framed-image` and `slide-text-polish` skills**, grafted from a sibling project's PPT toolkit (`.claude/skills/`, tests re-run after copy: 9/9 and 9/9) — available but **not yet wired into `pptx_export.py`**, nothing in the generator changed. `pptx-framed-image` clips an image to a template frame's exact preset shape (`prstGeom` cloning) instead of a plain rectangle, and ships `stock_images.py` — a real royalty-free photo fetcher (Openverse, CC0, no API key) with a procedural offline fallback (`nature_images.py`); relevant if a client-uploaded template (`build_presentation()`'s template path) turns out to carry its own photo-frame placeholders worth filling properly rather than leaving blank or stretching an image into. `slide-text-polish` lints slide copy (title-as-claim, bullet length, filler, abbreviations) — usable on any `{title, bullets}` structure regardless of the export path. `docs/vscode1-export/` is now a full local mirror of the sibling project VSCode1's `export/` folder (`ppt-toolkit.md`, `points-amelioration-ppt.md`, `design-system-octo.md`, `template-octo.md`, `optimisation-tokens.md`, 2026-07-15) — same convention as the sibling project VSCode3 (which mirrors the same 5 files). Reference copy, not source of truth — `template-octo.md`/`design-system-octo.md` describe VSCode1's own OCTO template/pipeline, useful here as design-principle inspiration since this project's PPT export also targets an OCTO-branded template, not as literal paths. Re-sync manually if the VSCode1 originals evolve.

**OpenHub agents integration** (`app/services/openhub_agents.py`) is unrelated to Claude Code's own skill/agent system — it shells out to an external `opencode` CLI (`.opencode/agents/`, `.opencode/skills/`) to power the app's own "Agents" page, with a simulated-response fallback when `opencode` isn't on PATH.

## Claude Code project setup

- `.claude/settings.json`, `.claude/skills/`, `.claude/hooks/` are versioned — shared by the whole team. `.claude/settings.local.json` and `CLAUDE.local.md` (personal preferences/notes, not created by default) are gitignored — never put secrets or machine-specific paths in the versioned files.
- A `PreToolUse` hook (`.claude/hooks/guard_destructive_git.py`) blocks `git push --force` (without `--force-with-lease`) and `git reset --hard` deterministically; `permissions.deny` blocks reading `.env`/`secrets/**`/`config/credentials.json`.
- Project skill `run-dev-server`: the verified way to launch the server, bootstrap a mission with real content via HTTP, and screenshot a page — use it instead of rediscovering that sequence.

## Skills & agents — comment ça se lance (post-BMAD, 2026-07-16)

Depuis l'install de **BMAD-METHOD v6.10.0** (`_bmad/`, config `_bmad/config.toml`), `.claude/skills/` contient 39 skills `bmad-*` **en plus** des skills projet (46 à l'install, 7 retirés par le tri exécuté le 2026-07-18 — `docs/reflexions/tri-skills-bmad.md`, restaurables via l'inventaire git). Repères pour ne pas se perdre parmi la quarantaine de skills :

- **Routeur BMAD** : en cas de doute sur quel skill BMAD lancer, invoquer **`bmad-help`** (analyse l'état et recommande le prochain skill).
- **Agents BMAD (personas)** : se lancent par nom — « parle à Amelia » (dev), « John » (PM), « Winston » (architecte), « Sally » (UX), « Mary » (analyste), « Paige » (tech writer). Ce sont les skills `bmad-agent-*`.
- **Workflows BMAD** : `bmad-product-brief` / `bmad-prd` / `bmad-architecture` / `bmad-create-epics-and-stories` / `bmad-create-story` / `bmad-dev-story` (cycle produit→dev) ; `bmad-code-review`, `bmad-retrospective`, `bmad-correct-course` (qualité/pilotage). Sorties dans `_bmad-output/` (candidat `.gitignore`).
- **Skills projet** (préexistants, non-`bmad-`) : `run-dev-server` (lancer/screenshoter l'app), `revue-increment` (definition-of-done avant commit — délègue à `bmad-code-review`/`bmad-retrospective`), `pptx-framed-image`, `slide-text-polish`. Les skills PPT globaux (`pptx-deck`, `pptx-verify`, `restitution-deck-design`) restent la référence pour l'export PPT.
- **Agents OpenHub** (`.opencode/agents`, via la CLI externe `opencode`) : indépendants du système de skills/agents ci-dessus — voir [`OPENHUB.md`](OPENHUB.md). Ne pas confondre avec les agents BMAD.
- Le hook **SessionStart** (`.claude/hooks/remind_revue_increment.py`) rappelle cette discipline à chaque session et route vers `bmad-help`.
- **Supervision des agents — étage 1 (2026-07-17) + étage 2 et challenge (2026-07-18)** : collecte déterministe de l'usage réel des skills/sous-agents (0 token LLM) via deux hooks — `PostToolUse` (matcher `Skill|Agent|Task` → journal `.claude/supervision/usage.jsonl`) et `SessionStart` (`.claude/supervision/scan_transcripts.py`, scan incrémental des transcripts `~/.claude/projects/<slug>/*.jsonl`) — qui régénère `docs/wiki/technical/agents-supervision.md` (tableau de bord + « TODO agents »), la section entre marqueurs `TODO-AGENTS` de `docs/wiki/index.md`, la section HTML entre marqueurs `TODO-AGENTS-HTML` de `docs/wiki.html` (rubrique « Supervision des agents » du sommaire), et `routing-hints.json` (cf. orchestrateur ci-dessous). **Pages générées, ne jamais les éditer à la main.** **Étage 2 — skill `agent-supervisor`** : diagnostic qualitatif à la demande (intégré à `revue-increment` §6, signalé par SessionStart si absent/périmé > 14 j) — lit uniquement les agrégats étage 1 + signaux git/mémoire (jamais les transcripts bruts : contenu d'interviews), écrit `.claude/supervision/diagnostic.json` via `write_diagnostic.py` (writer validé : `titre`/`preuve` requis par constat, 5 max) que le scan fusionne dans le wiki et les hints (`prudence`). **Challenge (incrément C)** : chaque constat peut porter une `proposition` de changement concret (rendue dans le wiki, jamais auto-appliquée — l'humain arbitre, l'orchestrateur applique) ; le scan y ajoute en déterministe la prudence sur échecs répétés (`runs.jsonl`), l'agrégat `trous_catalogue` (résolutions ad hoc récurrentes → TODO), la péremption du diagnostic à l'activité (3 runs non couverts) et une section OpenHub optionnelle (`agent_results` de `data/app.db`, lecture seule). **Arbitrages (2026-07-18, boucle propose→arbitre bouclée)** : `.claude/supervision/arbitrages.json` (versionné, édité à la main, jamais écrit par le scan) enregistre les décisions humaines qui closent un constat automatique (`cible` = skill ou `famille:<Nom>`) — le TODO correspondant disparaît des pages générées mais la décision reste affichée (section « Arbitrages enregistrés ») et fusionnée dans `routing-hints.json` ; l'usage réel reste mesuré (l'étage 2 peut re-challenger un arbitrage avec des données nouvelles). Premiers arbitrages : tri BMAD exécuté + les 3 skills PPT jamais invoquées conservées et reliées au playbook `export-ppt-verifie`. `state.json`/`usage.jsonl`/`diagnostic.json` sont gitignorés (données machine). Tests : `tests/test_agent_supervision.py`. Conception : `docs/reflexions/agent-superviseur.md` (incréments A/B/C livrés).
- **Orchestrateur — étages O-A + O-B (2026-07-17) + O-C (2026-07-18)** : la skill **`agent-orchestrator`** est le point d'entrée par défaut des demandes de travail multi-étapes/multi-agents — un hook `UserPromptSubmit` (`.claude/hooks/orchestrator_gate.py`) injecte à chaque prompt (hors commandes slash) une grille de qualification courte : orchestrer (plan avec mode cascade/parallèle/asynchrone et modèle par sous-agent, vérifications obligatoires insérées d'office, journal via `py .claude/orchestration/log_run.py`) ou exécuter directement sans en parler. Données de routage : `.claude/orchestration/catalogue.md` (recommandations, versionné) + `routing-hints.json` (généré à chaque session par le scan superviseur — éprouvés/jamais-utilisés/en-sommeil, vérifications oubliées, stats plan-vs-réel croisées avec `runs.jsonl`, fusion du futur `diagnostic.json` étage 2 ; gitignoré comme `runs.jsonl`) + la page supervision générée (version lisible). **O-B (playbooks)** : `.claude/orchestration/playbooks/` — format déclaratif (`FORMAT.md` : étapes typées agent/mode/modèle/contrat/checkpoint, garde-fou « playbooks morts » via un statut `eprouve`/`jamais-joue`) ; trois playbooks manuels tirés de la pratique réelle (`dev-verifie` : implémentation → tests → vérification réelle conditionnelle au type de fichiers → `revue-increment` avant commit ; `revue-design-parallele` : fan-out `Explore` ≤4 puis consolidation, pattern US9.12 ; `export-ppt-verifie`, 2026-07-18 : livrable = le deck — génération `pptx-deck` → étapes conditionnelles `pptx-framed-image`/`slide-text-polish`/`restitution-deck-design` (jamais invoquées, conservées par arbitrage et reliées ici) → `pptx-verify` obligatoire → `revue-increment` ; colonne vertébrale `eprouve`e, étapes conditionnelles à jouer avec prudence) ; `cycle-produit-bmad` généré depuis `_bmad/_config/bmad-help.csv` (`generate_bmad_playbook.py` — ne jamais éditer le `.md` à la main, regénérer), statut `jamais-joue` assumé, routé uniquement sur demande explicite. La skill cherche un playbook matchant avant de composer un plan à vide. **Mémoire git des agents (2026-07-18)** : si aucun agent/skill ne couvre la demande, `py .claude/orchestration/git_agents_inventory.py` inventorie tous les agents que git connaît, **présents et supprimés** (ex. les 26 agents `openhub_clone` retirés le 2026-07-16, avec commande de restauration `git show <sha>^:<chemin>`) — la skill propose alors restauration, évolution du plus proche, ou création via `skill-creator`, toujours arbitrée par l'utilisateur, résolution notée dans le journal. Tests : `tests/test_agent_orchestration.py` (structure des playbooks, terminaison `revue-increment`, régénération déterministe du playbook BMAD, inventaire git en repo jetable). Conception : `docs/reflexions/agent-orchestrateur.md` (O-C complété le 2026-07-18 avec la livraison d'`agent-supervisor`, cf. supervision ci-dessus).

## Discipline de gestion des tokens (2026-07-16, cf. `docs/vscode1-export/optimisation-tokens.md`)

Le contexte est un cache actif facturé à chaque tour, pas une mémoire gratuite — le laisser croître sans discipline pénalise coût, qualité (« lost in the middle ») et latence (source : OCTO Playbook Agentique, partie « Optimiser la consommation Tokens »). Règles concrètes, pas de changement de ton/style de réponse :

- **Ne pas parcourir** `_bmad/`, `_bmad-output/`, `.venv/`, `__pycache__/`, `.git/`, `data/`, `.pytest_cache/` sauf demande explicite — hors du périmètre utile à 99 % des tâches.
- **Lire avant d'écrire, grep les appelants avant de modifier** une fonction/un champ partagé (déjà la pratique de ce projet — l'expliciter la rend systématique).
- **Préférer un grep/read ciblé à un dump récursif** — surtout sur `.claude/skills/bmad-*` (39 skills) qui gonfle vite le contexte si parcouru en entier.
- **Sous-agent pour toute sortie volumineuse** (exploration large, longs logs) plutôt que de la laisser polluer le contexte principal.
- **`/compact` dès ~40 %** de fenêtre de contexte utilisée si la conversation doit continuer longtemps sur le même sujet.
